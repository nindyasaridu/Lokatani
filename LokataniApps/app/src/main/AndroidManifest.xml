<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.nindyasaridu.lokataniapps">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo_lokatani"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar">
        <activity android:name=".SplashScreenActivity">

            <!-- android:theme="@android:style/Theme.NoTitleBar.Fullscreen"> -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".LoginActivity"
            android:label="@string/title_activity_login" />
        <activity
            android:name=".RegisterActivity"
            android:label="@string/title_activity_register" />
        <activity android:name=".TambahJadwalActivity" />
        <activity android:name=".MenuActivity" />
        <!-- <activity -->
        <!-- android:name=".KelolaPanenActivity" -->
        <!-- android:label="@string/title_activity_kelola_panen" -->
        <!-- android:theme="@style/AppTheme.NoActionBar" /> -->
        <activity android:name=".HargaKomoditiActivity" />
        <activity android:name=".PrediksiKeuntunganActivity" />
        <activity android:name=".HasilPanenActivity" />
        <activity android:name=".DetilPrediksiActivity" />
        <activity android:name=".TambahAktifitasActivity"></activity>
    </application>

</manifest>